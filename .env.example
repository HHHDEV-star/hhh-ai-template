# Copy to .env and fill in your key. .env is gitignored.

# Anthropic API key — 給 object_qa demo 用 (生成延伸問題)
# 取得:https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-api03-xxx

# Replicate API token — 給 render demo 用 (平面圖 → 擬真渲染)
# 取得:https://replicate.com/account/api-tokens
REPLICATE_API_TOKEN=r8_xxx
